<% unless notice.blank? then %>
  <div class="d-flex alert alert-info"> 
   <div class="">
    <%= lucide_icon('speech') %> 
   </div>
   <div class="flex-grow-1 ps-2">
    <%= raw(notice) %>
   </div>
  </div>
<% end %>

<% unless alert.blank? then %>
  <div class="d-flex alert alert-info"> 
   <div class="">
    <%= lucide_icon('triangle-alert') %> 
   </div>
   <div class="flex-grow-1 ps-2">
    <%= raw(alert) %> 
   </div>
  </div>
<% end %>
